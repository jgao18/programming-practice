#https://www.hackerrank.com/challenges/ctci-merge-sort

'''
mergesort(array) {
    mergesort(left half)
    mergesort(right half)
    merge left and right half in sorted order, one by one
}

always O(nlogn) runtime, but O(n) space

